1.读取输入的geneID,从ysxgff.gff第10列（geneID）匹配相应的行，且行的第2列为CDS。从这些检索的行中取出
第3列（开始位置Start）和第4列的值（结束位置end）、第1列的值（染色体ID）、第5列的值（链向）。
2.从ysxhg19.fa检索出染色体ID中Start和end之间的值，链向如果为正，就输出读回的值，如果为负，就反向读
出并按互补输出。例如：如果取出的值为ATCGG，那么反向就是GGCTA，根据A->T,C->G互补规则，互补的结果是
CCGAT，此即为输出结果。